apiVersion: v1
kind: Secret
metadata:
  name: test-secret
  namespace: nginx
  annotations:
    avp.kubernetes.io/path: "tiqan/data/secret1"
type: kubernetes.io/basic-auth
stringData:
  username: <name> 